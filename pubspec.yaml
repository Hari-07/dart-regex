name: dart_regex
description: Type-safe DSL for building regular expressions in Dart using composable building blocks.
version: 0.0.1
repository: https://github.com/Hari-07/dart-regex
issue_tracker: https://github.com/Hari-07/dart-regex/issues


environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
